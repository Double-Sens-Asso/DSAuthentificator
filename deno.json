{
  "tasks": {
    "dev": "deno run -A --env --watch main.ts",
    "start": "deno run -A --env main.ts",
    "check": "deno check main.ts"
  },
  "compilerOptions": {
    "lib": ["deno.window", "dom"]
  }
}
